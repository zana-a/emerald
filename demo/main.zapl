mod Add do
  def f(): Int = 0
  def f(a: Int): Int = a
  #comment
  def f(a: Int, b: Int): Int = true # a + b
  def f(a: List<Int>): Int = Enum::sum(a)
end

Add::f()
Add::f(2)
Add::f(1, 4)
Add::f([1, 2, 3])